program: src/main.py
project: SINR
name: swinr/tcdcclm
entity: postech_sinr
method: bayes
metric:
  name: min_valid_loss
  goal: minimize
parameters:
  omega:
    min: 1.
    max: 10.
  sigma:
    min: 1.
    max: 10.
  hidden_dim:
    values: [128, 512]
  lr:
    min: 0.0001
    max: 0.01
  # max_order:
  #   values: [1, 2, 3, 4, 5]
command:
  - python
  - ${program}
  - ${args}
  - --dataset=tcdcclm
  - --model=swinr
  - --max_epoch=1000
  - --batch_size=4
  - --skip
  - --time
  - --in_memory
  - --plot