program: src/main_denoising.py
name: swinr
entity: postech_sinr
method: grid
parameters:
  omega:
    values: [0.1, 1, 10, 30]
  sigma:
    values: [0.1, 1, 10, 30]
  lr:
    values: [0.005, 0.001]
  hidden_dim:
    values: [128, 256, 512]
command:
  - python
  - ${program}
  - ${args}
  - --dataset_dir=dataset/sun360
  - --model=wire
  - --skip
  - --max_epoch=500 # depend on dataset
  - --panorama_idx=0
  - --normalize
  # - --batch_size=1 # only for temporal
  - --plot # only for spatial