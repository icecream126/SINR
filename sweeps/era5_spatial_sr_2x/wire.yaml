program: src/main_superres.py
name: wire
method: grid
parameters:
  omega:
    values: [0.1, 1]
  sigma:
    values: [0.1, 1]
  lr:
    values: [0.0001]
  downscale_factor:
    values: [2]
  batch_size:
    values: [4096]
  dataset_dir:
    values: ['dataset/spatial_0_25/era5_geopotential','dataset/spatial_0_25/era5_temperature']
  seed:
    values: [0,1,2]
command:
  - python
  - ${program}
  - ${args}
  - --input_dim=3
  - --model=wire
  - --skip
  - --project_name=era5_spatial
  - --max_epoch=500 # depend on dataset
  # - --panorama_idx=0
  - --normalize
  # - --batch_size=1 # only for temporal
  - --plot # only for spatial